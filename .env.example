# Supabase — required for the CRM and the /admin portal to boot at all.
# Values live in Vercel for production; copy them into .env.local to run locally.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
