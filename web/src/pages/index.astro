---
import BaseLayout from '../layouts/BaseLayout.astro';
import StatementPanel from '../components/StatementPanel.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

const heroLines = ['We architect', 'long-term', 'success.'];

const capabilities = [
  'React', 'Next.js', 'Astro', 'TypeScript', 'Node.js', 'PostgreSQL',
  'Supabase', 'Tailwind', 'GSAP', 'Figma', 'KiCad', 'Altium',
  'ESP32', 'STM32', 'LoRaWAN', 'MQTT', 'AWS', 'Vercel',
];

// Engagement kicker tells you the commercial shape before the description
// does — that is the thing a buyer is actually scanning for.
const services = [
  {
    kicker: 'Scoped product build',
    title: 'Web Development',
    href: '/services/development',
    body: 'React, Next.js and Astro applications that load fast, rank well, and hold up after launch. Built from scratch — no themes, no page builders.',
  },
  {
    kicker: 'Identity engagement',
    href: '/services/design',
    title: 'Design & Branding',
    body: 'Interface design, design systems, and visual identity. The system ships with the work, so your team can extend it without us.',
  },
  {
    kicker: 'Board-level engineering',
    href: '/services/iot',
    title: 'IoT & Embedded',
    body: 'Custom PCB design, firmware, and connected hardware. The part most agencies subcontract, we do in-house.',
  },
  {
    kicker: 'Workflow-led systems build',
    href: '/services/automation',
    title: 'AI & Automation',
    body: 'LLM integrations and internal tooling built around how your team already works, not around a demo.',
  },
  {
    kicker: 'Advisory engagement',
    href: '/services/consultancy',
    title: 'Technical Consultancy',
    body: 'Architecture reviews, stack decisions, and roadmaps — so you avoid the expensive mistake before you make it.',
  },
  {
    kicker: 'Ongoing retainer',
    href: '/services/support',
    title: 'Support & Growth',
    body: 'Maintenance, performance work, and iteration once the build ships. Optional, never bundled in to inflate a quote.',
  },
];

// A real sequence, so the numbering carries information the reader needs.
const process = [
  { n: '01', title: 'Scope', body: 'We sit with your team and map what actually needs building. You leave with a written scope and a number, free.' },
  { n: '02', title: 'Design', body: 'Interface and system design, reviewed with you weekly. Nothing gets built from a guess.' },
  { n: '03', title: 'Build', body: 'Two-week cycles with a working link at the end of each one. You watch it come together.' },
  { n: '04', title: 'Handover', body: 'Documented, audited, and yours. Every credential, every repo, every decision written down.' },
];

const work = [
  { client: 'Meridian Logistics', sector: 'Fleet telematics', result: 'Cut dispatch time 41%', body: 'Custom LoRaWAN trackers and a live routing dashboard — hardware and software from one team.' },
  { client: 'Aster Health', sector: 'Clinical SaaS', result: '3.2s → 0.8s load', body: 'Rebuilt a stalling patient portal on Next.js, with an audited migration and zero downtime.' },
  { client: 'Kiln & Co', sector: 'D2C commerce', result: '+36% conversion', body: 'Brand system, storefront, and the internal tooling to run it without a developer on call.' },
];

const differentiators = [
  { title: 'We learn the business first', body: 'Before any code, we map how your team actually operates. Most agencies skip this and build the wrong thing beautifully.' },
  { title: 'One team, one conversation', body: 'No account managers relaying messages. You talk to the people building your project, every time.' },
  { title: 'Honest scoping, not upselling', body: 'Sometimes the answer is a simple site, not a platform. We say so, even when it means a smaller invoice.' },
  { title: 'It works after we leave', body: 'You own everything and you understand everything. No hostage code, no mandatory retainer.' },
];

const industries = [
  { tag: 'Healthcare', label: 'Clinics & platforms' },
  { tag: 'Logistics', label: 'Fleet & telematics' },
  { tag: 'Manufacturing', label: 'Industrial IoT' },
  { tag: 'E-commerce', label: 'D2C & retail' },
  { tag: 'Education', label: 'Schools & LMS' },
  { tag: 'Energy', label: 'Metering & monitoring' },
  { tag: 'Fintech', label: 'Payments & portals' },
  { tag: 'Agriculture', label: 'Sensing & automation' },
  { tag: 'Software', label: 'Products & platforms' },
];

// The final value ships in the HTML so it is present for crawlers and for
// reduced motion; the counter only rewinds it and plays it forward.
const stats = [
  { v: '65+', count: 65, prefix: '', suffix: '+', l: 'Projects delivered across web, hardware and automation' },
  { v: '36%', count: 36, prefix: '', suffix: '%', l: 'Average client ROI in the first year after launch' },
  { v: '4', count: 4, prefix: '', suffix: '', l: 'Continents we ship to from one Karachi studio' },
];
---

<BaseLayout
  title="Xtenzium — We Architect Long-Term Success"
  description="Karachi-based digital agency building web products and the custom hardware underneath them. Web development, design, IoT, and AI automation."
  path="/"
>
  <Navbar />

  <main>
    <!-- ═══ 01 · Hero ═══════════════════════════════════════════ -->
    <section
      data-hero
      class="on-carbon relative flex min-h-screen items-end overflow-hidden bg-carbon pb-20 pt-36"
    >
      <canvas
        data-hero-canvas
        aria-hidden="true"
        class="pointer-events-none absolute inset-0 h-full w-full"></canvas>

      <div
        class="pointer-events-none absolute inset-0 bg-gradient-to-r from-carbon via-carbon/85 to-transparent"
      >
      </div>

      <div class="container-x relative z-10">
        <p data-hero-eyebrow class="eyebrow !text-white/55">
          Karachi, Pakistan — Software &amp; Hardware
        </p>

        <h1
          class="mt-5 max-w-4xl font-display text-[clamp(2.5rem,7vw,5.25rem)] font-bold leading-[1.0] tracking-[-0.035em] text-white"
        >
          {
            heroLines.map((line, i) => (
              <span data-hero-mask class="block overflow-hidden pb-2 -mb-2">
                <span data-hero-line class="block">
                  {i === 1 ? <span class="text-copper-hi">{line}</span> : line}
                </span>
              </span>
            ))
          }
        </h1>

        <p data-hero-body class="mt-8 max-w-xl text-lg leading-relaxed text-white/65">
          A digital agency building web products and the custom hardware
          underneath them. Sixty-five projects delivered for teams across four
          continents.
        </p>

        <div class="mt-10 flex flex-col items-start gap-3 sm:flex-row">
          <a
            data-hero-cta
            href="/work"
            class="inline-flex w-full items-center justify-center rounded-pill bg-copper px-7 py-3.5 text-sm font-bold text-white transition-colors duration-300 hover:bg-copper-hi sm:w-auto"
          >
            See the work
          </a>
          <a
            data-hero-cta
            href="/contact"
            class="inline-flex w-full items-center justify-center rounded-pill border border-white/60 bg-white/5 px-7 py-3.5 text-sm font-bold text-white backdrop-blur-sm transition-colors duration-300 hover:bg-white hover:text-carbon sm:w-auto"
          >
            Book a scoping call
          </a>
        </div>
      </div>
    </section>

    <!-- ═══ 02 · Capability marquee ═════════════════════════════ -->
    <section class="border-b border-line bg-bg py-7">
      <div class="marquee" aria-label="Technologies we work with">
        {
          [0, 1].map((dup) => (
            <div class="marquee__track" aria-hidden={dup === 1 ? 'true' : undefined}>
              {capabilities.map((c) => (
                <span class="font-mono text-sm whitespace-nowrap text-ink-3">{c}</span>
              ))}
            </div>
          ))
        }
      </div>
    </section>

    <!-- ═══ 03 · Services ═══════════════════════════════════════ -->
    <section id="services" class="bg-bg py-24 md:py-32">
      <div class="container-x">
        <div data-reveal-group class="max-w-2xl">
          <p class="eyebrow" data-reveal="eyebrow">What we do</p>
          <h2
            class="mt-3 font-display text-[clamp(1.75rem,4vw,2.75rem)] font-bold tracking-[-0.03em] text-ink"
          >
            <span class="block" data-reveal="line">Software and hardware,</span>
            <span class="block" data-reveal="line">under one roof.</span>
          </h2>
          <p class="mt-4 leading-relaxed text-ink-2" data-reveal="body">
            Most agencies stop at the browser. We keep going — through firmware,
            onto the board, and back again.
          </p>
        </div>

        <!-- Editorial rows, not a card grid. Six services in six identical
             boxes reads as a spec sheet; rows let the titles carry weight. -->
        <ol data-reveal-cards class="mt-14">
          {
            services.map((s, i) => (
              <li data-reveal="card">
                <a
                  href={s.href}
                  class="group grid items-baseline gap-x-8 gap-y-3 border-t border-line py-8 transition-colors duration-300 hover:border-copper md:grid-cols-[3rem_minmax(0,20rem)_1fr_2rem] md:py-9"
                >
                  <span class="font-mono text-xs text-ink-3">
                    {String(i + 1).padStart(2, '0')}
                  </span>

                  <div>
                    <p class="eyebrow !text-copper">{s.kicker}</p>
                    <h3 class="mt-2.5 font-display text-[clamp(1.5rem,3vw,2.125rem)] font-bold leading-[1.08] tracking-[-0.03em] text-ink transition-colors duration-300 group-hover:text-copper">
                      {s.title}
                    </h3>
                  </div>

                  <p class="max-w-lg leading-relaxed text-ink-2">{s.body}</p>

                  <span
                    aria-hidden="true"
                    class="hidden text-xl text-ink-3 transition-all duration-300 group-hover:translate-x-1 group-hover:text-copper md:block md:justify-self-end"
                  >
                    &rarr;
                  </span>
                </a>
              </li>
            ))
          }
          <li class="border-t border-line" />
        </ol>
      </div>
    </section>

    <!-- ═══ 04 · Statement — 'rise' ═════════════════════════════ -->
    <StatementPanel
      variant="rise"
      eyebrow="The difference"
      lines={['We build the', 'software and the', 'board it runs on.']}
      accentWord="board"
    />

    <!-- ═══ 05 · Process ════════════════════════════════════════ -->
    <section class="bg-bg-tint py-24 md:py-32">
      <div class="container-x">
        <div data-reveal-group class="max-w-2xl">
          <p class="eyebrow" data-reveal="eyebrow">How it goes</p>
          <h2
            class="mt-3 font-display text-[clamp(1.75rem,4vw,2.75rem)] font-bold tracking-[-0.03em] text-ink"
          >
            <span class="block" data-reveal="line">Four steps, and you</span>
            <span class="block" data-reveal="line">see work at every one.</span>
          </h2>
        </div>

        <!-- Nodes on a routed trace. The process is a sequence, so it gets
             a connector — and the connector is the same PCB language as
             the hero, which is what ties the page together. -->
        <ol data-reveal-cards class="relative mt-16 grid gap-10 sm:grid-cols-2 lg:grid-cols-4 lg:gap-6">
          <div
            aria-hidden="true"
            class="pointer-events-none absolute left-0 right-0 top-[7px] hidden h-px bg-gradient-to-r from-copper/40 via-copper/40 to-transparent lg:block"
          >
          </div>

          {
            process.map((p) => (
              <li data-reveal="card" class="relative">
                <span
                  aria-hidden="true"
                  class="absolute left-0 top-0 block h-[15px] w-[15px] rounded-full border-[3px] border-copper bg-bg-tint"
                />
                <div class="pt-11">
                  <span class="font-mono text-xs text-copper">{p.n}</span>
                  <h3 class="mt-3 font-display text-2xl font-bold tracking-[-0.03em] text-ink">
                    {p.title}
                  </h3>
                  <p class="mt-3 max-w-xs text-[0.95rem] leading-relaxed text-ink-2">{p.body}</p>
                </div>
              </li>
            ))
          }
        </ol>
      </div>
    </section>

    <!-- ═══ 06 · Selected work ══════════════════════════════════ -->
    <section class="bg-bg py-24 md:py-32">
      <div class="container-x">
        <div
          data-reveal-group
          class="flex flex-col gap-6 sm:flex-row sm:items-end sm:justify-between"
        >
          <div class="max-w-xl">
            <p class="eyebrow" data-reveal="eyebrow">Selected work</p>
            <h2
              class="mt-3 font-display text-[clamp(1.75rem,4vw,2.75rem)] font-bold tracking-[-0.03em] text-ink"
            >
              <span class="block" data-reveal="line">Proof, not promises.</span>
            </h2>
          </div>
          <a
            href="/work"
            data-reveal="body"
            class="shrink-0 rounded-pill border border-line-strong px-6 py-3 text-sm font-bold text-ink transition-colors duration-300 hover:bg-ink hover:text-bg"
          >
            View all case studies
          </a>
        </div>

        <div data-reveal-cards class="mt-14 grid gap-4 md:grid-cols-3">
          {
            work.map((w) => (
              <a
                href="/work"
                data-reveal="card"
                class="group flex flex-col rounded-card border border-line bg-surface p-8 transition-colors duration-300 hover:border-copper-line"
              >
                <p class="eyebrow">{w.sector}</p>
                <h3 class="mt-4 font-display text-xl font-bold tracking-[-0.025em] text-ink">
                  {w.client}
                </h3>
                <p class="mt-3 flex-1 text-[0.95rem] leading-relaxed text-ink-2">{w.body}</p>
                <p class="mt-6 font-display text-2xl font-bold tracking-[-0.03em] text-copper tabular-nums">
                  {w.result}
                </p>
              </a>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ═══ 07 · Stats ══════════════════════════════════════════ -->
    <!-- Numbers this big are the content. Boxing them shrinks them, so
         they run edge to edge on carbon and break the run of light. -->
    <section class="on-carbon bg-carbon py-20 md:py-24">
      <div class="container-x">
        <div data-reveal-group>
          <p class="eyebrow !text-white/45" data-reveal="eyebrow">Track record</p>
        </div>

        <div
          data-reveal-cards
          class="mt-10 grid divide-y divide-white/10 sm:mt-12 sm:grid-cols-3 sm:divide-x sm:divide-y-0"
        >
          {
            stats.map((s, i) => (
              <div
                data-reveal="card"
                class:list={['py-8 sm:py-0', i === 0 ? 'sm:pr-10' : 'sm:px-10', i === 2 && 'sm:pr-0']}
              >
                <div
                  data-count-to={s.count}
                  data-count-prefix={s.prefix}
                  data-count-suffix={s.suffix}
                  class="font-display text-[clamp(3.5rem,9vw,6.5rem)] font-bold leading-[0.9] tracking-[-0.05em] text-copper-hi tabular-nums"
                >
                  {s.v}
                </div>
                <p class="mt-4 max-w-[16rem] text-sm leading-relaxed text-white/55">{s.l}</p>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ═══ 08 · Why us ═════════════════════════════════════════ -->
    <section class="bg-bg py-24 md:py-32">
      <div class="container-x">
        <div data-reveal-group class="max-w-2xl">
          <p class="eyebrow" data-reveal="eyebrow">Why Xtenzium</p>
          <h2
            class="mt-3 font-display text-[clamp(1.75rem,4vw,2.75rem)] font-bold tracking-[-0.03em] text-ink"
          >
            <span class="block" data-reveal="line">Why it matters for</span>
            <span class="block" data-reveal="line">your business.</span>
          </h2>
        </div>

        <!-- Asymmetric bento on carbon. Equal boxes flatten four claims into
             a list; unequal ones give the argument a shape. Each tile is
             sized to take an editorial still behind the copy — see the
             shot list in the brief. -->
        <div
          data-reveal-cards
          class="mt-14 grid gap-3 md:grid-cols-2 md:grid-rows-[repeat(3,minmax(0,1fr))] lg:h-[34rem]"
        >
          {
            differentiators.map((d, i) => (
              <article
                data-reveal="card"
                class:list={[
                  'group relative flex flex-col justify-end overflow-hidden rounded-card border border-white/10 bg-carbon p-7 min-h-[13rem]',
                  i === 0 && 'md:row-span-2',
                  i === 3 && 'md:col-span-2 md:row-span-1',
                ]}
              >
                <div
                  aria-hidden="true"
                  class="pointer-events-none absolute -right-16 -top-16 h-56 w-56 rounded-full bg-copper/20 blur-3xl transition-opacity duration-500 group-hover:bg-copper/30"
                >
                </div>

                <div class="relative">
                  <h3 class="font-display text-xl font-bold tracking-[-0.025em] text-white">
                    {d.title}
                  </h3>
                  <p class="mt-3 max-w-md text-[0.95rem] leading-relaxed text-white/60">
                    {d.body}
                  </p>
                </div>
              </article>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ═══ 09 · Statement — 'focus' ════════════════════════════ -->
    <StatementPanel
      variant="focus"
      lines={['Your brand deserves', 'more than a template.']}
      accentWord="template"
    />

    <!-- ═══ 10 · Industries ═════════════════════════════════════ -->
    <section class="bg-bg-alt py-20 md:py-28">
      <div class="container-x">
        <div data-reveal-group class="max-w-2xl">
          <p class="eyebrow" data-reveal="eyebrow">Industries</p>
          <h2
            class="mt-3 font-display text-[clamp(1.75rem,4vw,2.75rem)] font-bold tracking-[-0.03em] text-ink"
          >
            <span class="block" data-reveal="line">Built for every business.</span>
          </h2>
        </div>

        <!-- Breadth is the message here, so the treatment is density:
             one flowing band of tags rather than nine equal boxes. -->
        <div data-reveal-cards class="mt-12 flex flex-wrap gap-2.5">
          {
            industries.map((i) => (
              <div
                data-reveal="card"
                class="group flex items-baseline gap-3 rounded-pill border border-line bg-surface py-3 pl-5 pr-6 transition-colors duration-300 hover:border-copper hover:bg-copper-lo"
              >
                <span class="eyebrow !text-copper">{i.tag}</span>
                <span class="text-sm text-ink-2">{i.label}</span>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- ═══ 11 · Contact — graduated commitment ═════════════════ -->
    <section id="contact" class="bg-bg py-24 md:py-32">
      <div class="container-x">
        <div data-reveal-group class="max-w-3xl">
          <p class="eyebrow" data-reveal="eyebrow">Start here</p>
          <h2
            class="mt-3 font-display text-[clamp(2rem,5vw,3.5rem)] font-bold tracking-[-0.035em] text-ink"
          >
            <span class="block" data-reveal="line">Tell us what you're</span>
            <span class="block" data-reveal="line">trying to build.</span>
          </h2>
          <p class="mt-5 max-w-lg text-lg leading-relaxed text-ink-2" data-reveal="body">
            Most projects start months before anyone signs anything. Pick
            whichever step you're actually on — none of them commit you.
          </p>
        </div>

        <!-- Three rungs, lightest first. A single "contact us" is the wrong
             ask when the average decision runs 3-18 months across 6-10 people. -->
        <div data-reveal-cards class="mt-12 grid gap-4 md:grid-cols-3">
          <div data-reveal="card" class="flex flex-col rounded-card border border-line bg-surface p-8">
            <p class="eyebrow">Just looking</p>
            <h3 class="mt-4 font-display text-xl font-bold tracking-[-0.025em] text-ink">
              Read a case study
            </h3>
            <p class="mt-3 flex-1 text-[0.95rem] leading-relaxed text-ink-2">
              How we scoped, what it cost, what broke, and what it returned. No
              form, no email required.
            </p>
            <a
              href="/work"
              class="mt-6 inline-flex items-center justify-center rounded-pill border border-line-strong px-6 py-3 text-sm font-bold text-ink transition-colors duration-300 hover:bg-ink hover:text-bg"
            >
              Browse the work
            </a>
          </div>

          <div data-reveal="card" class="flex flex-col rounded-card border border-line bg-surface p-8">
            <p class="eyebrow">Comparing options</p>
            <h3 class="mt-4 font-display text-xl font-bold tracking-[-0.025em] text-ink">
              Get a ballpark
            </h3>
            <p class="mt-3 flex-1 text-[0.95rem] leading-relaxed text-ink-2">
              Answer four questions and get a realistic range by email — enough
              to take to whoever holds the budget.
            </p>
            <a
              href="/estimate"
              class="mt-6 inline-flex items-center justify-center rounded-pill border border-line-strong px-6 py-3 text-sm font-bold text-ink transition-colors duration-300 hover:bg-ink hover:text-bg"
            >
              Estimate a project
            </a>
          </div>

          <div
            data-reveal="card"
            class="on-carbon flex flex-col rounded-card border border-carbon bg-carbon p-8"
          >
            <p class="eyebrow !text-copper-hi">Ready to talk</p>
            <h3 class="mt-4 font-display text-xl font-bold tracking-[-0.025em] text-white">
              Book a scoping call
            </h3>
            <p class="mt-3 flex-1 text-[0.95rem] leading-relaxed text-white/60">
              Thirty minutes with the people who'd build it. You leave with a
              written scope and a number, free either way.
            </p>
            <a
              href="/contact"
              class="mt-6 inline-flex items-center justify-center rounded-pill bg-copper px-6 py-3 text-sm font-bold text-white transition-colors duration-300 hover:bg-copper-hi"
            >
              Pick a time
            </a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>
